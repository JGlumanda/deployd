name: docs
on:
  push:
    branches: [main]
    paths: ['docs/**', 'mkdocs.yml', '.github/workflows/docs.yml']
  workflow_dispatch:

jobs:
  docs:
    uses: Caprimos/.github/.github/workflows/techdocs-publish.yml@main
    with:
      entity: default/component/caprimos-site
    secrets: inherit
